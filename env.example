# Balkan File Management System Environment Configuration
# Copy this file to .env and update the values for your environment

# ===========================================
# BACKEND CONFIGURATION
# ===========================================

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=balkan_task
DB_USER=postgres
DB_PASSWORD=postgres123
DB_SSLMODE=disable

# JWT Configuration (REQUIRED - Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-32-chars-minimum

# Storage Configuration
STORAGE_PATH=./uploads

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================

# API Configuration
VITE_API_URL=http://localhost:8080

# ===========================================
# DEVELOPMENT NOTES
# ===========================================

# For Docker Development:
# - DB_HOST should be 'postgres' (container name)
# - SERVER_HOST should be '0.0.0.0' (listen on all interfaces)
# - VITE_API_URL should point to backend container

# For Local Development:
# - DB_HOST should be 'localhost'
# - SERVER_HOST should be 'localhost'
# - VITE_API_URL should be 'http://localhost:8080'

# Security Checklist:
# ✅ Change JWT_SECRET to a long random string
# ✅ Use strong database passwords
# ✅ Set DB_SSLMODE=require for production
# ✅ Configure proper firewall rules
# ✅ Use HTTPS in production
